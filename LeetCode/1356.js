var sortByBits = function (arr) {
  let bitArr = arr.map((v) => v.toString(2));

  const countOne = bitArr.arr(/1/g);
  return bits ? bits.length : 0;

  console.log(countOne);
};
console.log(sortByBits([0, 1, 2, 3, 4, 5, 6, 7, 8]));
